version: '3.8'

services:
  auth-service:
    build:
      context: ../../
      dockerfile: services/auth-service/Containerfile
    container_name: platform-auth-service
    restart: always 
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - SERVER_PORT=8080
      - ENVIRONMENT=dev
      - LOG_LEVEL=debug
      - DATABASE_HOST=platform-postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - DATABASE_NAME=auth_db
      - DATABASE_SSL_MODE=disable
      - REDIS_ADDRESS=platform-redis:6379
      - REDIS_PASSWORD=devredispass
      - JWT_SECRET=dev-secret-change-in-production-minimum-32-chars!
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  organization-service:
    build:
      context: ../../
      dockerfile: services/organization-service/Containerfile
    container_name: platform-organization-service
    restart: always 
    ports:
      - "127.0.0.1:8081:8081"
    environment:
      - SERVER_PORT=8081
      - ENVIRONMENT=dev
      - LOG_LEVEL=debug
      - DATABASE_HOST=platform-postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - DATABASE_NAME=org_db
      - DATABASE_SSL_MODE=disable
      - REDIS_ADDRESS=platform-redis:6379
      - REDIS_PASSWORD=devredispass
      - REDIS_DB=1
      - JWT_SECRET=dev-secret-change-in-production-minimum-32-chars!
      - AUTH_SERVICE_BASE_URL=http://platform-auth-service:8080
    depends_on:
      - auth-service
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8081/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  notification-service:
    build:
      context: ../../
      dockerfile: services/notification-service/Containerfile
    container_name: platform-notification-service
    restart: always
    ports:
      - "127.0.0.1:8082:8082"
    environment:
      - SERVER_PORT=8082
      - ENVIRONMENT=dev
      - LOG_LEVEL=debug
      - DATABASE_HOST=platform-postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - DATABASE_NAME=notification_db
      - DATABASE_SSL_MODE=disable
      - REDIS_ADDRESS=platform-redis:6379
      - REDIS_PASSWORD=devredispass
      - REDIS_DB=2
      - SMTP_HOST=localhost
      - SMTP_PORT=1025
      - SMTP_FROM_ADDRESS=noreply@example.com
      - SMTP_FROM_NAME=Microservices Platform
    depends_on:
      - auth-service
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8082/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  audit-service:
    build:
      context: ../../
      dockerfile: services/audit-service/Containerfile
    container_name: platform-audit-service
    restart: always
    ports:
      - "127.0.0.1:8085:8085"
    environment:
      - SERVER_PORT=8085
      - ENVIRONMENT=dev
      - LOG_LEVEL=debug
      - DATABASE_HOST=platform-postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - DATABASE_NAME=audit_db
      - DATABASE_SSL_MODE=disable
      - REDIS_ADDRESS=platform-redis:6379
      - REDIS_PASSWORD=devredispass
      - REDIS_DB=4
      - COMPLIANCE_HASHCHAININGENABLED=true
    depends_on:
      - auth-service
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8085/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  billing-service:
    build:
      context: ../../
      dockerfile: services/billing-service/Containerfile
    container_name: platform-billing-service
    restart: always
    ports:
      - "127.0.0.1:8083:8083"
    environment:
      - APP_SERVER__HOST=0.0.0.0
      - APP_SERVER__PORT=8083
      - APP_DATABASE__HOST=platform-postgres
      - APP_DATABASE__PORT=5432
      - APP_DATABASE__USER=postgres
      - APP_DATABASE__PASSWORD=postgres
      - APP_DATABASE__DATABASE=billing_db
      - APP_DATABASE__SSL_MODE=disable
      - APP_DATABASE__MAX_CONNECTIONS=10
      - APP_DATABASE__MIN_CONNECTIONS=2
      - APP_REDIS__HOST=platform-redis
      - APP_REDIS__PORT=6379
      - APP_REDIS__PASSWORD=devredispass
      - APP_REDIS__DB=3
      - APP_STRIPE__SECRET_KEY=sk_test_placeholder
      - APP_STRIPE__WEBHOOK_SECRET=whsec_placeholder
      - APP_STRIPE__PUBLISHABLE_KEY=pk_test_placeholder
      - APP_STRIPE__TEST_MODE=true
      - APP_AUTH__JWT_SECRET=dev-secret-change-in-production-minimum-32-chars!
      - APP_LOGGER__LEVEL=debug
      - APP_LOGGER__ENVIRONMENT=dev
    depends_on:
      - auth-service
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 45s

  file-service:
    build:
      context: ../../
      dockerfile: services/file-service/Containerfile
    container_name: platform-file-service
    restart: always
    ports:
      - "127.0.0.1:8084:8084"
    environment:
      - APP_SERVER__HOST=0.0.0.0
      - APP_SERVER__PORT=8084
      - APP_DATABASE__HOST=platform-postgres
      - APP_DATABASE__PORT=5432
      - APP_DATABASE__USER=postgres
      - APP_DATABASE__PASSWORD=postgres
      - APP_DATABASE__DATABASE=file_db
      - APP_DATABASE__SSL_MODE=disable
      - APP_DATABASE__MAX_CONNECTIONS=10
      - APP_DATABASE__MIN_CONNECTIONS=2
      - APP_REDIS__HOST=platform-redis
      - APP_REDIS__PORT=6379
      - APP_REDIS__PASSWORD=devredispass
      - APP_REDIS__DB=5
      - APP_S3__ENDPOINT=http://platform-minio:9000
      - APP_S3__REGION=us-east-1
      - APP_S3__ACCESS_KEY=minioadmin
      - APP_S3__SECRET_KEY=minioadmin
      - APP_S3__BUCKET_PREFIX=platform
      - APP_AUTH__JWT_SECRET=dev-secret-change-in-production-minimum-32-chars!
      - APP_LOGGER__LEVEL=debug
      - APP_LOGGER__ENVIRONMENT=dev
    depends_on:
      - auth-service
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8084/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 45s

networks:
  app-net:
    external: true
    name: podman_app-net
  data-net:
    external: true
    name: podman_data-net
