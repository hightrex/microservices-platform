version: '3.8'

services:
  auth-service:
    build:
      context: ../../
      dockerfile: services/auth-service/Containerfile
    container_name: platform-auth-service
    restart: always 
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - SERVER_PORT=8080
      - ENVIRONMENT=dev
      - LOG_LEVEL=debug
      - DATABASE_HOST=platform-postgres
      - DATABASE_PORT=5432
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=postgres
      - DATABASE_NAME=auth_db
      - DATABASE_SSL_MODE=disable
      - REDIS_ADDRESS=platform-redis:6379
      - REDIS_PASSWORD=devredispass
      - JWT_SECRET=dev-secret-change-in-production-minimum-32-chars!
    networks:
      - app-net
      - data-net
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 512m
    cpus: 0.5
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

networks:
  app-net:
    external: true
    name: podman_app-net
  data-net:
    external: true
    name: podman_data-net
