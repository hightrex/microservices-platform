{
  "name": "@microservices-platform/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript library for the microservices platform — logger, errors, health, tenant, types",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit --detectOpenHandles",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.13.4",
    "jest": "^30.0.0",
    "ts-jest": "^29.4.0",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "pino": "^9.6.0"
  }
}
